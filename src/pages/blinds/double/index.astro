---
import Layout from "@layouts/Layout.astro";
import Navbar from "@components/navbar/navbar";
import InnerBanner from "@components/inner-banner/innerBanner";
import ShopComponent from "@components/shop/Shop";
import Footer from "@components/Footer/Footer";
import fetchMedusaApi from "@lib/lib/fetchMedusaApi";


const productsData = await fetchMedusaApi<any>({
  endpoint: "store/products/custom",
});
const filteredProducts = (productsData?.products || []).filter((product) => {
  const isSingleType = product.type?.value === "double";
  const hasBlindsCategory = product.categories?.some(
    (cat) => cat.name?.toLowerCase() === "blinds"
  );

  return isSingleType && hasBlindsCategory;
});
const tags = [
  'Blockout',
  'Light Filtering',
  'Sunscreen',
];


---

<Layout>
      <Navbar client:load logo="dark"/>
      <InnerBanner client:load title="Double Roller BLINDS SHOP" content="Beautiful Designer Double Roller Blinds You Can Install in 20 Minutes"/>
      <ShopComponent client:load data={filteredProducts} customizePage="blinds/double" tags={tags} type="double"/>
      <Footer client:visible/>
</Layout>