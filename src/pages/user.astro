---
import Layout from "@layouts/Layout.astro";
import Navbar from "@components/navbar/navbar";
import Users from "@components/user/user";
import Footer from "@components/Footer/Footer";

// import fetchMedusaApi from '../lib/lib/fetchMedusaApi';

// async function getOrders() {
//   // LocalStorage se email uthao (agar browser me chal raha hai)
//   let email = "";
//   if (typeof window !== "undefined") {
//     email = localStorage.getItem("userEmail") || "";
//   }

//   if (!email) {
//     console.error("User email not found in localStorage!");
//     return null;
//   }

//   const orders = await fetchMedusaApi<any>({
//     endpoint: "/store/customers/order",
//     query: { email }, // yahan localStorage ka email use hoga
//   });

//   return orders;
// }

// const orders = await getOrders();
// console.log("Orders:", orders);



---

<Layout>
	<Navbar client:load logo="dark"/>
	<Users client:load/>
	<Footer client:load/>
</Layout>